package com.alim.ssn.test;

import android.content.Context;

import com.alim.ssn.R;
import com.alim.ssn.model.Post;

import java.util.ArrayList;
import java.util.List;

public class FakePostGenerator {
  /*  public static List<Post> getPosts(Context context){
        List<Post> posts=new ArrayList<>();
        for (int i = 0; i < 20; i++) {
            Post post=new Post();
            post.setId(i);
            post.setContent(context.getString(R.string.lorem_ipsum));
            post.setActive(true);
            post.setCommentsCount((long)(Math.random()*(500-10+1)+10));//give a number between 500 and 10 => (Math.random() * ((max - min) + 1)) + min
            post.setLikesCount((long)(Math.random()*(500-10+1)+10));
            post.setStudentId("bXfY1FAhtReWuHpXrEnygbUt7Ll1");
            post.setImagePath("https://www.automatetheplanet.com/wp-content/uploads/2015/06/Test-URL-Redirects-WebDriver.jpg");
            posts.add(post);
        }
        return posts;
    }*/
}
